<!-- 001 - Largest 3-Same-Digit Number in String -->

Good problem for basic building logic

Code:

string largestGoodInteger(string num) {
        
        int n = num.size();
        char maxchar=' ';
        int maxValue = -1;
        for(int i=2;i<n;i++)
        {
           if(num[i] == num[i-1] && num[i-1] == num[i-2])
           {
               int tempval = num[i] - '0';
               if(tempval>maxValue)
               {
                  maxValue = tempval;
                  maxchar = num[i];
               }
           }
        }

        if(maxchar == ' ') return "";
        return string(3,maxchar);
    }